# 0.2.0 refactor - session handoff

State of the hexagonal refactor on `refactor/hexagonal-core`, written so
work resumes cold. Companion docs: `deferred-decisions.md` (same dir).

## Done (commit trail)

| Commit | What |
|---|---|
| `16caeee` | fact-carrying error taxonomy (`StorageError`, errno, templates) |
| `2e2868f` | port DTOs (`Entry`, `RawStat`), `Capabilities`, `PathKind` |
| `9e8be59` | strict ruff ruleset adopted, tree brought in line |
| `fdf53fe` | backend port: `StorageBackend` Protocol + `BackendBase` + `generic.py` + `MemoryBackend` |
| `c1b5d9f` | unasync codegen pipeline (`scripts/unasync.py`, `--check`, generated tests) |
| `734ba33` | `_compat.gather` shim; parallel `du`/`delete_tree` |
| `b4535d6` | `pathops.py` pure lexical resolution |
| `5e49f56` | `Storix` core (unix API: variadic `path, /, *paths`, `rm -r`/strict `rmdir`, mv/cp last-is-dest, ls hides dotfiles) |
| `4895c9e` | `LocalBackend` (hand twin pair) + conformance suite parametrized over backends |
| `eddfe42` | `AzureBackend` (HNS-only) + port `close()` + Storix ctx manager + integration lane |

Suite: 623 passing (memory + local, both flavors), 68 azure integration
params deselected by default. Regenerate with
`uv run python scripts/unasync.py`; never edit `_sync/**` by hand
(hand-twin exceptions: `_compat.py`, `local.py`, `azure.py`, `test_compat.py`).

## Remaining: CLI rewrite (last item) + azure shakedown

CLI: rewrite cli/app.py + shell.py on the sync Storix/get_storage
(typed errors, variadic signatures, ls hidden default); restore an
entry point (__main__.py was removed with the legacy tree); drop the
cli/** lint exemption when done. Docs site decision (zensical?) comes
after; README already carries the quickstart.

## ~~Azure integration shakedown~~ DONE: 80/80 passed on a real HNS account

The Azure backend is wire-unverified. Run:

```bash
ADLSG2_ACCOUNT_NAME=... ADLSG2_TOKEN=... uv run pytest -m integration -q
```

Expect first-contact failures around dfs error codes (`_ERROR_CODE_MAP`
in both azure twins), `make_dir(parents=True)` with a file mid-chain,
and rename target syntax (`container/key`). Fix in the async twin,
mirror to sync by hand.

## Step 9 - layers, factory, CLI (next up)

1. ~~`SandboxLayer`~~ DONE (`70fea41`): `_async/layers.py`, codegen'd,
   passes the full conformance suite as the 'sandbox' fixture param;
   errors re-scoped to virtual paths (`from None` so the real prefix
   never leaks). No `Storix(sandboxed=)` sugar - explicit composition
   only. `PathError` base made public for layer rescoping (`9be22bf`).
2. ~~`storix.temporary()`~~ DONE (`70fea41`): async+generated-sync
   context manager, mkdtemp-rooted LocalBackend, deleted on exit.
3. ~~Factory + config~~ DONE (`fc8bebd`): decisions taken - STORIX_*
   clean break (no legacy aliases), `get_storage()` only (no
   classmethod shortcuts), zero-config default = local at `~/.storix`.
   `storix/config.py` (pydantic-settings per backend) +
   `_async/factory.py` (codegen'd) + `ConfigurationError`; env.example
   rewritten. URL-scheme factory stays 0.3.0.
4. **CLI rewire** (the one remaining step-9 item): `cli/` consumes the
   sync `Storix` + new `get_storage`; adapt call sites to typed-error
   raising (no more bool returns), variadic signatures, ls hidden
   default. `cli/shell.py` REPL same. Behavior-preserving otherwise.

## ~~Step 10 - teardown~~ DONE (`2d3d396`)

Delete: `providers/`, `aio/`, `sandbox.py`, old `factory.py` pair,
`PathLogicMixin` in `utils/__init__.py`, `tests/legacy/`, the transition
per-file-ignores block in pyproject (grep `transition debt`), legacy
`__init__.py` lazy-import machinery (rewire to `_sync`/`_async` +
`storix.backends`/`storix.aio.backends` re-exports). Then:
`FileProperties.file_kind` -> `kind` rename (TODO in models.py), release
notes migration table, version bump to 0.2.0.

## Slice 6c - optional, nothing blocks on it

`tree`/`find`/`wc` core commands. Plan: async-walk paths in the core,
feed `Tree.from_iterable`; `find` over the walked list; `wc` over `cat`.

## URLs + metadata: DONE (`dff6940`)

`fs.url(expires_in=)` / port `make_url` (capability-gated, Azure SAS,
raising default on BackendBase), `fs.data_url()` core-generic,
`write(metadata=)` + `RawStat.metadata`/`FileProperties.metadata`
(memory + azure advertise it; local via future MetadataLayer). Factory
now has typed overloads (Unpack'd TypedDicts) and `register_backend()`.
Azure make_url/set_metadata are wire-unverified (integration lane).

## Parking lot (conformance decisions pending)

`delete('/')` / `rm -r` of cwd or root (azure refuses via
`UnsupportedOperationError`, memory/local currently allow); stale yields
under concurrent deletes; `mv` of a path into itself; symlink escape in
LocalBackend; container auto-creation (AzureBackend requires existing).

## Resume ritual

```bash
git log --oneline -12          # confirm state matches the table above
uv run python scripts/unasync.py --check && uv run ruff check . && uv run pytest -q
```
